/**
 * At or below this unit count a product is flagged as low stock, giving the
 * three bands the brief specifies: in stock (>5), low stock (1–5), out (0).
 */
export const LOW_STOCK_THRESHOLD = 5

/** Rows per page in the product tables. */
export const PAGE_SIZE = 8
