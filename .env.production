# The production build has no JSON Server behind it — Mock Service Worker
# intercepts these calls in the browser (see src/mocks/). A same-origin path
# keeps the requests relative, so the deployment works on any domain.
#
# To point a production build at a real hosted API instead, override this with
# an absolute URL in the host's environment variables.
VITE_API_URL=/api
